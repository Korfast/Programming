<Window x:Class="Contacts.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Contacts.View.Controls"
        xmlns:viewmodel="clr-namespace:Contacts.ViewModel;assembly=Contacts.ViewModel"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodel:MainVM}"
        Title="Contacts" Height="450" Width="700">
    <Window.Resources>
        <!-- Шаблон элемента списка контактов -->
        <DataTemplate x:Key="ContactTemplate">
            <Grid Margin="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="40" Height="40" 
                        Background="LightGray" CornerRadius="20" Margin="3"/>
                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                    <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Phone}" FontSize="11" Foreground="Gray"/>
                    <TextBlock Text="{Binding Email}" FontSize="11" Foreground="Gray"/>
                </StackPanel>
            </Grid>
        </DataTemplate>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibility"/>
    </Window.Resources>

    <Grid Margin="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="200"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="2*" MinWidth="300"/>
        </Grid.ColumnDefinitions>

        <!-- Левая колонка -->
        <Grid Grid.Column="0" Background="#f8f8f8">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Поле поиска -->
                <Grid Grid.Row="0" Margin="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Content="Find:" Margin="0,0,3,0" Padding="0" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="0"/>
                </Grid>

                <!-- Список контактов -->
                <ListBox Grid.Row="1" ItemsSource="{Binding Contacts}" 
                         SelectedItem="{Binding SelectedContact}" 
                         ItemTemplate="{StaticResource ContactTemplate}"
                         Margin="3" Padding="0"/>

                <!-- Кнопки -->
                <Grid Grid.Row="2" Margin="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Add" Height="25" Margin="0,3,3,3" 
                            Command="{Binding AddCommand}" IsEnabled="{Binding IsAddEditRemoveEnabled}"/>
                    <Button Grid.Column="1" Content="Edit" Height="25" Margin="3,3,3,3" 
                            Command="{Binding EditCommand}" IsEnabled="{Binding IsEditRemoveEnabled}"/>
                    <Button Grid.Column="2" Content="Remove" Height="25" Margin="3,3,0,3" 
                            Command="{Binding RemoveCommand}" IsEnabled="{Binding IsEditRemoveEnabled}"/>
                </Grid>
            </Grid>
        </Grid>

        <!-- Разделитель -->
        <GridSplitter Grid.Column="1" Width="10" HorizontalAlignment="Stretch" Background="White"/>

        <!-- Правая колонка -->
        <Grid Grid.Column="2" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <controls:ContactControl Grid.Row="0" 
                                     DataContext="{Binding CurrentContact}"
                                     IsReadOnly="{Binding DataContext.IsReadOnly, 
                                                RelativeSource={RelativeSource AncestorType=Window}}"/>

            <Button Grid.Row="1" Content="Apply" Width="75" Height="25"
                    Command="{Binding ApplyCommand}" HorizontalAlignment="Left" 
                    Margin="10,50,0,10"
                    Visibility="{Binding IsApplyVisible, Converter={StaticResource BooleanToVisibility}}"/>
        </Grid>
    </Grid>
</Window>