<UserControl x:Class="Contacts.View.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Стиль для TextBox с валидацией -->
        <Style x:Key="ErrorStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="3"/>
            <Setter Property="Margin" Value="3"/>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="ToolTip" 
                            Value="{Binding (Validation.Errors).CurrentItem.ErrorContent, 
                                    RelativeSource={RelativeSource Self}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="1" Content="Name:" Margin="3" Padding="0" VerticalAlignment="Center"/>

        <Border Grid.Row="1" Grid.Column="0" Grid.RowSpan="5" Width="100" Height="100" 
                Background="LightGray" Margin="3" VerticalAlignment="Top">
            <TextBlock Text="Photo" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <TextBox Grid.Row="1" Grid.Column="1" 
                 Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged,
                        ValidatesOnDataErrors=True, NotifyOnValidationError=True}"
                 Style="{StaticResource ErrorStyle}"
                 IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

        <Label Grid.Row="2" Grid.Column="1" Content="Phone Number:" Margin="3" Padding="0" VerticalAlignment="Center"/>

        <TextBox x:Name="PhoneTextBox" Grid.Row="3" Grid.Column="1" 
                 Text="{Binding Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged,
                        ValidatesOnDataErrors=True, NotifyOnValidationError=True}"
                 Style="{StaticResource ErrorStyle}"
                 IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 PreviewTextInput="OnPreviewTextInput"
                 DataObject.Pasting="OnPasting"/>

        <Label Grid.Row="4" Grid.Column="1" Content="Email:" Margin="3" Padding="0" VerticalAlignment="Center"/>

        <TextBox Grid.Row="5" Grid.Column="1" 
                 Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged,
                        ValidatesOnDataErrors=True, NotifyOnValidationError=True}"
                 Style="{StaticResource ErrorStyle}"
                 IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
    </Grid>
</UserControl>